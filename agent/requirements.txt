# Genesis backend (the project's only LLM backend). Cross-platform: Windows/macOS/Linux.
# No Google API key is ever used.
requests>=2.31
python-dotenv>=1.0
pydantic>=2.7
fastapi>=0.110
uvicorn>=0.29
